---
import Layout from '../../layouts/Layout.astro';
import {
  Settings,
  CheckCircle,
  ArrowRight,
  Phone,
  Shield,
  AlertTriangle,
  Calendar
} from 'lucide-astro';

// Ensure base always ends with a slash
const baseUrl = import.meta.env.BASE_URL;
const base = baseUrl.endsWith('/') ? baseUrl : `${baseUrl}/`;

const benefits = [
  'Frühzeitige Erkennung von Leckagen',
  'Schutz vor Umweltschäden',
  'Erfüllung gesetzlicher Prüfpflichten',
  'Verlängerung der Anlagenlebensdauer',
  'Dokumentation für Behörden',
  'Kostenersparnis durch Prävention'
];

const process = [
  {
    title: 'Sichtprüfung',
    description: 'Visuelle Kontrolle der LAG-Auskleidung auf Beschädigungen'
  },
  {
    title: 'Sensortest',
    description: 'Funktionsprüfung aller Leckanzeigegeräte und Sensoren'
  },
  {
    title: 'Dichtigkeitsprüfung',
    description: 'Test der Zwischenraum-Dichtigkeit mit zugelassenen Verfahren'
  },
  {
    title: 'Dokumentation',
    description: 'Erstellung des Prüfprotokolls und ggf. Mängelberichte'
  }
];

const checkItems = [
  'Leckschutzauskleidung (LAG)',
  'Leckanzeigegeräte',
  'Überfüllsicherung',
  'Grenzwertgeber',
  'Füllstandsanzeige',
  'Leitungen und Anschlüsse'
];
---

<Layout
  title="LAG-Wartung Krefeld | Leckschutzauskleidung Prüfung | IS-Tankschutz"
  description="Professionelle LAG-Wartung in Krefeld und Umgebung. Regelmäßige Prüfung Ihrer Leckschutzauskleidung für maximale Sicherheit. WHG-zertifizierter Fachbetrieb."
  keywords="LAG Wartung Krefeld, Leckschutzauskleidung Prüfung, Tankprüfung, Leckanzeige Wartung, WHG Prüfung, Düsseldorf, NRW"
>
  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-dark-950 via-dark-900 to-dark-950 py-20 lg:py-28">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid lg:grid-cols-2 gap-12 items-center">
        <div>
          <div class="flex items-center gap-2 text-primary mb-4">
            <a href={`${base}leistungen`} class="hover:underline">Leistungen</a>
            <span>/</span>
            <span>LAG-Wartung</span>
          </div>
          <h1 class="text-4xl sm:text-5xl font-bold text-white mb-6">
            Professionelle <span class="text-primary">LAG-Wartung</span>
          </h1>
          <p class="text-dark-300 text-lg mb-8">
            Die Leckschutzauskleidung (LAG) ist ein wichtiges Sicherheitselement Ihrer
            Tankanlage. Regelmäßige Wartung und Prüfung stellen sicher, dass dieser
            Schutz zuverlässig funktioniert – für Ihre Sicherheit und die Umwelt.
          </p>
          <div class="flex flex-col sm:flex-row gap-4">
            <a
              href={`${base}kontakt`}
              class="inline-flex items-center justify-center gap-2 bg-primary hover:bg-primary-700 text-white px-8 py-4 rounded-lg font-semibold transition-all"
            >
              Wartung vereinbaren
              <ArrowRight class="w-5 h-5" />
            </a>
            <a
              href="tel:+4921519329640"
              class="inline-flex items-center justify-center gap-2 bg-white/10 hover:bg-white/20 text-white px-8 py-4 rounded-lg font-semibold transition-all border border-white/20"
            >
              <Phone class="w-5 h-5" />
              02151 9329640
            </a>
          </div>
        </div>

        <!-- Service Image Placeholder -->
        <div class="hidden lg:block">
          <div class="bg-gradient-to-br from-primary/20 to-primary/5 rounded-2xl aspect-square flex items-center justify-center border border-primary/20">
            <Settings class="w-32 h-32 text-primary/50" />
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- What is LAG Section -->
  <section class="py-20 lg:py-28 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid lg:grid-cols-2 gap-16 items-center">
        <div>
          <span class="text-primary font-semibold text-sm uppercase tracking-wider">Was ist LAG?</span>
          <h2 class="text-3xl sm:text-4xl font-bold text-dark mt-4 mb-6">
            Leckschutzauskleidung erklärt
          </h2>
          <p class="text-dark-500 text-lg mb-6">
            Die Leckschutzauskleidung (LAG) ist eine zusätzliche Sicherheitseinrichtung
            bei Heizöltanks. Sie besteht aus einer flüssigkeitsdichten Innenhülle, die
            austretende Flüssigkeit auffängt und über ein Leckanzeigegerät meldet.
          </p>
          <p class="text-dark-500 mb-8">
            Bei einwandigen Tanks in Wasserschutzgebieten oder Überschwemmungsgebieten
            ist eine LAG oft vorgeschrieben. Aber auch außerhalb dieser Gebiete bietet
            sie wertvolle zusätzliche Sicherheit.
          </p>

          <div class="bg-yellow-50 border border-yellow-200 rounded-xl p-6">
            <div class="flex items-start gap-4">
              <AlertTriangle class="w-6 h-6 text-yellow-600 flex-shrink-0 mt-1" />
              <div>
                <h3 class="font-semibold text-dark mb-2">Prüfpflicht beachten!</h3>
                <p class="text-dark-600 text-sm">
                  LAG-Systeme müssen regelmäßig durch einen Fachbetrieb geprüft werden.
                  Bei Versäumnis drohen Bußgelder und Versicherungsprobleme.
                </p>
              </div>
            </div>
          </div>
        </div>

        <div class="bg-gray-50 rounded-2xl p-8">
          <h3 class="text-xl font-bold text-dark mb-6">Was wird geprüft?</h3>
          <ul class="space-y-4">
            {checkItems.map((item) => (
              <li class="flex items-center gap-3">
                <CheckCircle class="w-5 h-5 text-primary flex-shrink-0" />
                <span class="text-dark-600">{item}</span>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Process Section -->
  <section class="py-20 lg:py-28 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center max-w-3xl mx-auto mb-16">
        <span class="text-primary font-semibold text-sm uppercase tracking-wider">Der Ablauf</span>
        <h2 class="text-3xl sm:text-4xl font-bold text-dark mt-4 mb-6">
          So läuft die LAG-Wartung ab
        </h2>
        <p class="text-dark-500 text-lg">
          Eine gründliche Prüfung aller sicherheitsrelevanten Komponenten.
        </p>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
        {process.map((step, index) => (
          <div class="bg-white rounded-xl p-6 shadow-sm">
            <div class="w-12 h-12 bg-primary text-white rounded-full flex items-center justify-center text-xl font-bold mb-4">
              {index + 1}
            </div>
            <h3 class="text-lg font-bold text-dark mb-2">{step.title}</h3>
            <p class="text-dark-500 text-sm">{step.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Benefits Section -->
  <section class="py-20 lg:py-28 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid lg:grid-cols-2 gap-16 items-center">
        <div class="order-2 lg:order-1">
          <div class="bg-gray-50 rounded-2xl p-8">
            <h3 class="text-xl font-bold text-dark mb-6">Ihre Vorteile</h3>
            <ul class="space-y-4">
              {benefits.map((benefit) => (
                <li class="flex items-center gap-3">
                  <CheckCircle class="w-5 h-5 text-primary flex-shrink-0" />
                  <span class="text-dark-600">{benefit}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>

        <div class="order-1 lg:order-2">
          <span class="text-primary font-semibold text-sm uppercase tracking-wider">Regelmäßige Wartung</span>
          <h2 class="text-3xl sm:text-4xl font-bold text-dark mt-4 mb-6">
            Vorsorge statt Nachsorge
          </h2>
          <p class="text-dark-500 text-lg mb-6">
            Eine defekte LAG bemerken Sie erst, wenn es zu spät ist. Regelmäßige
            Wartung stellt sicher, dass im Ernstfall alles funktioniert und Sie
            vor teuren Schäden geschützt sind.
          </p>

          <div class="grid grid-cols-2 gap-4">
            <div class="bg-primary/10 rounded-xl p-4 text-center">
              <Shield class="w-8 h-8 text-primary mx-auto mb-2" />
              <p class="text-sm font-semibold text-dark">Maximaler Schutz</p>
            </div>
            <div class="bg-primary/10 rounded-xl p-4 text-center">
              <Calendar class="w-8 h-8 text-primary mx-auto mb-2" />
              <p class="text-sm font-semibold text-dark">Alle 2-5 Jahre</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 lg:py-28 bg-primary">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-3xl sm:text-4xl font-bold text-white mb-6">
        Wann war Ihre letzte LAG-Prüfung?
      </h2>
      <p class="text-white/80 text-lg mb-10 max-w-2xl mx-auto">
        Vereinbaren Sie jetzt einen Wartungstermin und stellen Sie sicher,
        dass Ihr Leckschutz zuverlässig funktioniert.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href={`${base}kontakt`}
          class="inline-flex items-center justify-center gap-2 bg-white hover:bg-gray-100 text-primary px-8 py-4 rounded-lg font-semibold text-lg transition-all"
        >
          Wartung vereinbaren
          <ArrowRight class="w-5 h-5" />
        </a>
        <a
          href="tel:+4921519329640"
          class="inline-flex items-center justify-center gap-2 bg-transparent hover:bg-white/10 text-white px-8 py-4 rounded-lg font-semibold text-lg transition-all border-2 border-white"
        >
          <Phone class="w-5 h-5" />
          02151 9329640
        </a>
      </div>
    </div>
  </section>
</Layout>
