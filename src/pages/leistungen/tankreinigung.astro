---
import Layout from '../../layouts/Layout.astro';
import {
  Droplets,
  CheckCircle,
  ArrowRight,
  Phone,
  Clock,
  Shield,
  Sparkles,
  AlertTriangle
} from 'lucide-astro';

// Ensure base always ends with a slash
const baseUrl = import.meta.env.BASE_URL;
const base = baseUrl.endsWith('/') ? baseUrl : `${baseUrl}/`;

const benefits = [
  'Verlängerung der Tanklebensdauer',
  'Verbesserung der Heizölqualität',
  'Vermeidung von Heizungsausfällen',
  'Schutz vor Korrosionsschäden',
  'Frühzeitige Schadenserkennung',
  'Dokumentation für Versicherungen'
];

const process = [
  {
    title: 'Entleerung',
    description: 'Absaugen des Restöls und der Schlammablagerungen mit Spezialfahrzeug'
  },
  {
    title: 'Reinigung',
    description: 'Gründliche Innenreinigung des Tanks mit Hochdrucktechnik'
  },
  {
    title: 'Inspektion',
    description: 'Sichtprüfung auf Risse, Korrosion und Undichtigkeiten'
  },
  {
    title: 'Befüllung',
    description: 'Rückführung des gereinigten Öls oder Neubefüllung'
  }
];
---

<Layout
  title="Tankreinigung Krefeld | Professionelle Heizöltank Reinigung | IS-Tankschutz"
  description="Professionelle Tankreinigung in Krefeld und Umgebung. Entfernung von Ölschlamm und Ablagerungen, verlängerte Tanklebensdauer. WHG-zertifizierter Fachbetrieb. Jetzt Termin vereinbaren!"
  keywords="Tankreinigung Krefeld, Heizöltank reinigen, Ölschlamm entfernen, Tank säubern, Düsseldorf, Duisburg, Mönchengladbach, NRW"
>
  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-dark-950 via-dark-900 to-dark-950 py-20 lg:py-28">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid lg:grid-cols-2 gap-12 items-center">
        <div>
          <div class="flex items-center gap-2 text-primary mb-4">
            <a href={`${base}leistungen`} class="hover:underline">Leistungen</a>
            <span>/</span>
            <span>Tankreinigung</span>
          </div>
          <h1 class="text-4xl sm:text-5xl font-bold text-white mb-6">
            Professionelle <span class="text-primary">Tankreinigung</span>
          </h1>
          <p class="text-dark-300 text-lg mb-8">
            Schützen Sie Ihre Heizungsanlage vor Schäden durch Ablagerungen und Ölschlamm.
            Unsere professionelle Tankreinigung verlängert die Lebensdauer Ihres Tanks und
            sichert eine optimale Heizölqualität.
          </p>
          <div class="flex flex-col sm:flex-row gap-4">
            <a
              href={`${base}kontakt`}
              class="inline-flex items-center justify-center gap-2 bg-primary hover:bg-primary-700 text-white px-8 py-4 rounded-lg font-semibold transition-all"
            >
              Termin vereinbaren
              <ArrowRight class="w-5 h-5" />
            </a>
            <a
              href="tel:+4921584058777"
              class="inline-flex items-center justify-center gap-2 bg-white/10 hover:bg-white/20 text-white px-8 py-4 rounded-lg font-semibold transition-all border border-white/20"
            >
              <Phone class="w-5 h-5" />
              02158 / 40 58 777
            </a>
          </div>
        </div>

        <!-- Service Image Placeholder -->
        <div class="hidden lg:block">
          <div class="bg-gradient-to-br from-primary/20 to-primary/5 rounded-2xl aspect-square flex items-center justify-center border border-primary/20">
            <Droplets class="w-32 h-32 text-primary/50" />
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Why Section -->
  <section class="py-20 lg:py-28 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid lg:grid-cols-2 gap-16 items-center">
        <div>
          <span class="text-primary font-semibold text-sm uppercase tracking-wider">Warum Tankreinigung?</span>
          <h2 class="text-3xl sm:text-4xl font-bold text-dark mt-4 mb-6">
            Sauberer Tank – sichere Heizung
          </h2>
          <p class="text-dark-500 text-lg mb-6">
            Im Laufe der Jahre bilden sich in jedem Heizöltank Ablagerungen aus Ölschlamm,
            Kondenswasser und Sedimenten. Diese Rückstände können zu ernsthaften Problemen führen:
          </p>

          <div class="bg-yellow-50 border border-yellow-200 rounded-xl p-6 mb-8">
            <div class="flex items-start gap-4">
              <AlertTriangle class="w-6 h-6 text-yellow-600 flex-shrink-0 mt-1" />
              <div>
                <h3 class="font-semibold text-dark mb-2">Risiken bei vernachlässigter Tankreinigung:</h3>
                <ul class="text-dark-600 text-sm space-y-1">
                  <li>• Verstopfte Filter und Leitungen</li>
                  <li>• Beschädigung der Brennerdüse</li>
                  <li>• Korrosion der Tankinnenwand</li>
                  <li>• Heizungsausfall im Winter</li>
                </ul>
              </div>
            </div>
          </div>

          <p class="text-dark-500">
            <strong>Unsere Empfehlung:</strong> Lassen Sie Ihren Tank alle 5-7 Jahre professionell reinigen.
            Bei älteren Tanks oder sichtbaren Verunreinigungen auch häufiger.
          </p>
        </div>

        <div class="bg-gray-50 rounded-2xl p-8">
          <h3 class="text-xl font-bold text-dark mb-6">Ihre Vorteile</h3>
          <ul class="space-y-4">
            {benefits.map((benefit) => (
              <li class="flex items-center gap-3">
                <CheckCircle class="w-5 h-5 text-primary flex-shrink-0" />
                <span class="text-dark-600">{benefit}</span>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Process Section -->
  <section class="py-20 lg:py-28 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center max-w-3xl mx-auto mb-16">
        <span class="text-primary font-semibold text-sm uppercase tracking-wider">Unser Verfahren</span>
        <h2 class="text-3xl sm:text-4xl font-bold text-dark mt-4 mb-6">
          So läuft die Tankreinigung ab
        </h2>
        <p class="text-dark-500 text-lg">
          Unser bewährtes Verfahren sorgt für eine gründliche Reinigung bei minimalem Aufwand für Sie.
        </p>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
        {process.map((step, index) => (
          <div class="bg-white rounded-xl p-6 shadow-sm">
            <div class="w-12 h-12 bg-primary text-white rounded-full flex items-center justify-center text-xl font-bold mb-4">
              {index + 1}
            </div>
            <h3 class="text-lg font-bold text-dark mb-2">{step.title}</h3>
            <p class="text-dark-500 text-sm">{step.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="py-20 lg:py-28 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid md:grid-cols-3 gap-8">
        <div class="text-center p-8">
          <div class="w-16 h-16 bg-primary/10 rounded-2xl flex items-center justify-center mx-auto mb-6">
            <Sparkles class="w-8 h-8 text-primary" />
          </div>
          <h3 class="text-xl font-bold text-dark mb-3">Modernste Technik</h3>
          <p class="text-dark-500">
            Hochleistungs-Absaugfahrzeuge und spezielle Reinigungstechnik für optimale Ergebnisse.
          </p>
        </div>
        <div class="text-center p-8">
          <div class="w-16 h-16 bg-primary/10 rounded-2xl flex items-center justify-center mx-auto mb-6">
            <Shield class="w-8 h-8 text-primary" />
          </div>
          <h3 class="text-xl font-bold text-dark mb-3">WHG-zertifiziert</h3>
          <p class="text-dark-500">
            Alle Arbeiten erfolgen nach den Vorschriften des Wasserhaushaltsgesetzes durch geschulte Fachkräfte.
          </p>
        </div>
        <div class="text-center p-8">
          <div class="w-16 h-16 bg-primary/10 rounded-2xl flex items-center justify-center mx-auto mb-6">
            <Clock class="w-8 h-8 text-primary" />
          </div>
          <h3 class="text-xl font-bold text-dark mb-3">Schnelle Ausführung</h3>
          <p class="text-dark-500">
            Die meisten Reinigungen sind innerhalb weniger Stunden abgeschlossen – ohne lange Wartezeiten.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 lg:py-28 bg-primary">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-3xl sm:text-4xl font-bold text-white mb-6">
        Wann wurde Ihr Tank zuletzt gereinigt?
      </h2>
      <p class="text-white/80 text-lg mb-10 max-w-2xl mx-auto">
        Vereinbaren Sie jetzt einen Termin für eine professionelle Tankreinigung.
        Wir erstellen Ihnen gerne ein unverbindliches Angebot.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href={`${base}kontakt`}
          class="inline-flex items-center justify-center gap-2 bg-white hover:bg-gray-100 text-primary px-8 py-4 rounded-lg font-semibold text-lg transition-all"
        >
          Jetzt Termin vereinbaren
          <ArrowRight class="w-5 h-5" />
        </a>
        <a
          href="tel:+4921584058777"
          class="inline-flex items-center justify-center gap-2 bg-transparent hover:bg-white/10 text-white px-8 py-4 rounded-lg font-semibold text-lg transition-all border-2 border-white"
        >
          <Phone class="w-5 h-5" />
          02158 / 40 58 777
        </a>
      </div>
    </div>
  </section>
</Layout>
