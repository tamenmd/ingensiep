---
import Layout from '../layouts/Layout.astro';
import ConsentMap from '../components/ConsentMap.astro';
import {
  Phone,
  Mail,
  MapPin,
  Clock,
  Send,
  MessageSquare,
  Printer,
  CheckCircle
} from 'lucide-astro';
import { formConfig, company, googleMaps } from '../content/config';

// Ensure base always ends with a slash
const baseUrl = import.meta.env.BASE_URL;
const base = baseUrl.endsWith('/') ? baseUrl : `${baseUrl}/`;

const services = [
  { value: 'tankreinigung', label: 'Tankreinigung' },
  { value: 'tanksanierung', label: 'Tanksanierung' },
  { value: 'tankdemontage', label: 'Tankdemontage' },
  { value: 'tankstilllegung', label: 'Tankstilllegung' },
  { value: 'tankneubau', label: 'Tankneubau' },
  { value: 'lag-wartung', label: 'LAG-Wartung' },
  { value: 'sonstiges', label: 'Sonstiges' }
];

// Formspree URL - Registrieren Sie sich auf formspree.io und ersetzen Sie die ID in src/content/config.ts
const formAction = formConfig.formspreeId !== 'YOUR_FORMSPREE_ID'
  ? `https://formspree.io/f/${formConfig.formspreeId}`
  : '#';
---

<Layout
  title="Kontakt | Angebot anfordern | Ingensiep & Schallenberg Grefrath"
  description="Kontaktieren Sie Ingensiep & Schallenberg für ein unverbindliches Angebot. Tankreinigung, Tanksanierung und mehr in Grefrath, Krefeld und Umgebung. Tel: 02158 4058777"
  keywords="Kontakt Ingensiep Schallenberg, Tankschutz Grefrath Kontakt, Angebot Tankreinigung, Beratung Tankservice, Krefeld, Mönchengladbach"
>
  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-dark-950 via-dark-900 to-dark-950 py-20 lg:py-28">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl">
        <span class="text-primary font-semibold text-sm uppercase tracking-wider">Kontakt</span>
        <h1 class="text-4xl sm:text-5xl font-bold text-white mt-4 mb-6">
          Wir sind für Sie da
        </h1>
        <p class="text-dark-300 text-lg">
          Haben Sie Fragen oder möchten ein Angebot anfordern? Kontaktieren Sie uns
          telefonisch, per E-Mail oder über unser Kontaktformular. Wir melden uns
          schnellstmöglich bei Ihnen zurück.
        </p>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section class="py-20 lg:py-28 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid lg:grid-cols-3 gap-12">
        <!-- Contact Info -->
        <div class="lg:col-span-1">
          <h2 class="text-2xl font-bold text-dark mb-8">Kontaktdaten</h2>

          <div class="space-y-6">
            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center flex-shrink-0">
                <Phone class="w-6 h-6 text-primary" />
              </div>
              <div>
                <h3 class="font-semibold text-dark mb-1">Telefon</h3>
                <a href={`tel:${company.phone.mainFormatted}`} class="text-dark-500 hover:text-primary transition-colors block">
                  {company.phone.main} <span class="text-dark-400 text-sm">(Grefrath)</span>
                </a>
                <a href={`tel:${company.phone.krefeldFormatted}`} class="text-dark-500 hover:text-primary transition-colors block text-sm">
                  {company.phone.krefeld} <span class="text-dark-400">(Krefeld)</span>
                </a>
                <a href={`tel:${company.phone.moenchengladbachFormatted}`} class="text-dark-500 hover:text-primary transition-colors block text-sm">
                  {company.phone.moenchengladbach} <span class="text-dark-400">(Mönchengladbach)</span>
                </a>
              </div>
            </div>

            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center flex-shrink-0">
                <Printer class="w-6 h-6 text-primary" />
              </div>
              <div>
                <h3 class="font-semibold text-dark mb-1">Fax</h3>
                <p class="text-dark-500">{company.phone.fax}</p>
              </div>
            </div>

            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center flex-shrink-0">
                <Mail class="w-6 h-6 text-primary" />
              </div>
              <div>
                <h3 class="font-semibold text-dark mb-1">E-Mail</h3>
                <a href={`mailto:${company.email}`} class="text-dark-500 hover:text-primary transition-colors">
                  {company.email}
                </a>
                <p class="text-sm text-dark-400 mt-1">Antwort innerhalb 24h</p>
              </div>
            </div>

            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center flex-shrink-0">
                <MapPin class="w-6 h-6 text-primary" />
              </div>
              <div>
                <h3 class="font-semibold text-dark mb-1">Adresse</h3>
                <p class="text-dark-500">
                  {company.name}<br />
                  {company.address.street}<br />
                  {company.address.zip} {company.address.city}
                </p>
              </div>
            </div>

            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-primary/10 rounded-xl flex items-center justify-center flex-shrink-0">
                <Clock class="w-6 h-6 text-primary" />
              </div>
              <div>
                <h3 class="font-semibold text-dark mb-1">Öffnungszeiten</h3>
                <p class="text-dark-500">
                  {company.openingHours.weekdays}<br />
                  {company.openingHours.friday}<br />
                  <span class="text-primary font-medium">{company.openingHours.emergency}</span>
                </p>
              </div>
            </div>
          </div>

          <!-- Emergency Box -->
          <div class="mt-8 p-6 bg-primary rounded-xl text-white">
            <h3 class="font-bold text-lg mb-2">Notfall?</h3>
            <p class="text-white/80 text-sm mb-4">
              Bei Ölaustritt oder anderen Notfällen erreichen Sie uns rund um die Uhr.
            </p>
            <a
              href={`tel:${company.phone.mainFormatted}`}
              class="inline-flex items-center gap-2 bg-white text-primary px-4 py-2 rounded-lg font-semibold text-sm hover:bg-gray-100 transition-colors"
            >
              <Phone class="w-4 h-4" />
              {company.phone.main}
            </a>
          </div>
        </div>

        <!-- Contact Form -->
        <div class="lg:col-span-2">
          <div class="bg-gray-50 rounded-2xl p-8 lg:p-12">
            <div class="flex items-center gap-3 mb-8">
              <MessageSquare class="w-8 h-8 text-primary" />
              <h2 class="text-2xl font-bold text-dark">Nachricht senden</h2>
            </div>

            <form action={formAction} method="POST" class="space-y-6" id="contact-form">
              <div class="grid sm:grid-cols-2 gap-6">
                <div>
                  <label for="name" class="block text-sm font-medium text-dark mb-2">
                    Name *
                  </label>
                  <input
                    type="text"
                    id="name"
                    name="name"
                    required
                    class="w-full px-4 py-3 bg-white border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-colors"
                    placeholder="Ihr vollständiger Name"
                  />
                </div>
                <div>
                  <label for="phone" class="block text-sm font-medium text-dark mb-2">
                    Telefon *
                  </label>
                  <input
                    type="tel"
                    id="phone"
                    name="phone"
                    required
                    class="w-full px-4 py-3 bg-white border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-colors"
                    placeholder="Ihre Telefonnummer"
                  />
                </div>
              </div>

              <div class="grid sm:grid-cols-2 gap-6">
                <div>
                  <label for="email" class="block text-sm font-medium text-dark mb-2">
                    E-Mail *
                  </label>
                  <input
                    type="email"
                    id="email"
                    name="email"
                    required
                    class="w-full px-4 py-3 bg-white border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-colors"
                    placeholder="ihre@email.de"
                  />
                </div>
                <div>
                  <label for="plz" class="block text-sm font-medium text-dark mb-2">
                    PLZ / Ort
                  </label>
                  <input
                    type="text"
                    id="plz"
                    name="plz"
                    class="w-full px-4 py-3 bg-white border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-colors"
                    placeholder="z.B. 47803 Krefeld"
                  />
                </div>
              </div>

              <div>
                <label for="service" class="block text-sm font-medium text-dark mb-2">
                  Gewünschte Leistung
                </label>
                <select
                  id="service"
                  name="service"
                  class="w-full px-4 py-3 bg-white border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-colors"
                >
                  <option value="">Bitte wählen...</option>
                  {services.map((service) => (
                    <option value={service.value}>{service.label}</option>
                  ))}
                </select>
              </div>

              <div>
                <label for="message" class="block text-sm font-medium text-dark mb-2">
                  Ihre Nachricht *
                </label>
                <textarea
                  id="message"
                  name="message"
                  rows="5"
                  required
                  class="w-full px-4 py-3 bg-white border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary transition-colors resize-none"
                  placeholder="Beschreiben Sie Ihr Anliegen..."
                ></textarea>
              </div>

              <div class="flex items-start gap-3">
                <input
                  type="checkbox"
                  id="privacy"
                  name="privacy"
                  required
                  class="mt-1 w-4 h-4 text-primary border-gray-300 rounded focus:ring-primary"
                />
                <label for="privacy" class="text-sm text-dark-500">
                  <span class="font-medium">DSGVO-Einverständnis:</span> Ich habe die <a href={`${base}datenschutz`} class="text-primary hover:underline">Datenschutzerklärung</a> gelesen und bin mit der Verarbeitung meiner Daten einverstanden. *
                </label>
              </div>

              <button
                type="submit"
                class="w-full sm:w-auto inline-flex items-center justify-center gap-2 bg-primary hover:bg-primary-700 text-white px-8 py-4 rounded-lg font-semibold transition-all hover:shadow-lg hover:shadow-primary/25"
              >
                <Send class="w-5 h-5" />
                Nachricht senden
              </button>
            </form>

            <p class="text-dark-400 text-sm mt-6">
              * Pflichtfelder. Wir melden uns in der Regel innerhalb von 24 Stunden bei Ihnen.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Map Section -->
  <section class="py-20 lg:py-28 bg-gray-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center max-w-3xl mx-auto mb-12">
        <span class="text-primary font-semibold text-sm uppercase tracking-wider">Einzugsgebiet</span>
        <h2 class="text-3xl sm:text-4xl font-bold text-dark mt-4 mb-6">
          Wir kommen zu Ihnen
        </h2>
        <p class="text-dark-500 text-lg">
          Von Grefrath aus bedienen wir den gesamten Großraum im Umkreis von 100 km.
          Krefeld, Düsseldorf, Duisburg, Mönchengladbach, Neuss und alle umliegenden Städte.
        </p>
      </div>

      <!-- Google Maps Embed (with Consent) -->
      <div class="shadow-lg rounded-2xl overflow-hidden">
        <ConsentMap
          src={googleMaps.embedUrl}
          title="Standort Ingensiep & Schallenberg GmbH"
          height="450"
        />
      </div>
    </div>
  </section>
</Layout>
